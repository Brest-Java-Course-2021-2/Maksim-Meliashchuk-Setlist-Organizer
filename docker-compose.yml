version: "3"

services:

  postgres:
    image: postgres:14
    container_name: postgres
    ports:
      - 5433:5432
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: setlistOrganizer
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - .:/var/lib/postgresql/data

  rest-app:
    build: ./rest-app
    image: docker-rest-app
    container_name: rest-app
    ports:
      - 8088:8088
    environment:
      POSTGRES_HOST: postgres
    depends_on:
      - postgres
    command: [ "java", "-jar", "-Dspring.profiles.active=postgres, jpa", "rest-app.jar" ]

  web-app:
    build: ./web-app
    image: docker-web-app
    container_name: web-app
    ports:
      - 8080:8080
    environment:
      HOST_NAME: rest-app
    depends_on:
      - rest-app
    network_mode: host

  grafana:
    build: ./rest-app/config
    ports:
      - 3000:3000
    volumes:
      - ./rest-app/config/datasource.yml:/etc/grafana/provisioning/datasources/datasource.yaml
    environment:
      GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH: "/var/lib/grafana/dashboards/jvm-micrometer_rev9.json"
    network_mode: host

  prometheus:
    image: prom/prometheus
    user: root
    ports:
      - 9090:9090
    volumes:
      - ./rest-app/config/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus:/prometheus
    network_mode: host